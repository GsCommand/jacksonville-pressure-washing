name: Update Pool Cage Hero

on:
  push:
    paths:
      - '.github/workflows/update-pool-cage-hero.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update pool cage hero image
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('House-Washing-Jacksonville-FL/pool-cage-cleaning/index.html')
          s = p.read_text()
          old = '<img src="/nocatee-roof-washing.webp" alt="Pool cage cleaning service in Jacksonville, Florida">'
          new = '<img src="/images/pool-cage-enclosure-cleaning.jpeg" alt="Pool cage cleaning service in Jacksonville, Florida">'
          if old not in s:
              raise SystemExit('Expected pool cage hero image markup not found')
          p.write_text(s.replace(old, new, 1))
          PY
      - name: Commit update and remove one-time workflow
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add House-Washing-Jacksonville-FL/pool-cage-cleaning/index.html
          git rm .github/workflows/update-pool-cage-hero.yml
          git commit -m 'Update pool cage cleaning hero image'
          git push